/* MCD1 · fifth de-bias pass — final marginal prose offenders.
 * (Code/value-comparison answers like m1-117, m1-125, m1-154 are left as-is:
 *  there the length reflects syntax, not verbosity, so it isn't a tell.) */
module.exports = {
  questions: {
    "m1-069": { optionEdits: {
      1: "It's a stream; the log shows the cursor object until it's consumed",
      3: "The payload was encrypted by a security policy applied before the logging step" } },
    "m1-073": { optionEdits: {
      1: "SLA tiers + SLA rate-limiting with client ID enforcement" } },
    "m1-096": { optionEdits: {
      1: "It exposes the WSDL's operations; a Transform before Consume shows the body" } },
    "m1-126": { optionEdits: {
      0: "It serves the interactive API console generated from the RAML" } },
    "m1-156": { optionEdits: {
      0: "A parent type, so it also catches HTTP: and DB:CONNECTIVITY children",
      1: "It matches only errors whose type is literally MULE:CONNECTIVITY, nothing else" } },
    "m1-184": { optionEdits: {
      0: "Only total > 1000 — top-down, first true condition wins" } },
    "m1-190": { optionEdits: {
      0: "The All validator grouping several validators",
      3: "Validators cannot be combined together into one component" } },
    "m1-212": { optionEdits: {
      0: "9090 — -D properties override file values",
      3: "Deployment fails at startup due to the property conflict" } },
    "m1-213": { optionEdits: {
      0: "The file name uses ${env}; deployment passes -Denv=dev/prod",
      2: "The runtime detects the environment automatically from the server hostname" } },
    "m1-218": { optionEdits: {
      0: "A Mule domain with the shared HTTPS Listener config; both apps reference it" } }
  }
};
