/* MCPA length-balance · final flip. After trimming, the correct option was still
 * marginally the longest in ~88% of questions — a residual "pick the longest"
 * tell. Extend the top distractor just past the correct answer (with a plausible,
 * still-wrong clause) in ~50 questions so length no longer predicts the answer. */
module.exports = {
  questions: {
    "pa-054": { optionEdits: { 0: "Mule event variables held in flight while the request is being processed" } },
    "pa-061": { optionEdits: { 0: "Fail immediately with a 500 error whenever the backend is unavailable or slow" } },
    "pa-008": { optionEdits: { 0: "A container-based deployment technology for running Mule apps on a Kubernetes cluster you manage" } },
    "pa-089": { optionEdits: { 1: "A rate limiting policy set very low so that fewer requests ever reach the backend system" } },
    "pa-006": { optionEdits: { 2: "A larger Exchange entitlement so the teams can hold many more published assets between them" } },
    "pa-026": { optionEdits: { 2: "Have both channels call the System APIs directly and orchestrate all the steps themselves" } },
    "pa-042": { optionEdits: { 0: "Proxies support a larger set of policies than embedded autodiscovery enforcement does" } },
    "pa-018": { optionEdits: { 3: "It cannot be prevented in REST without pessimistic server-side record locking during edits" } },
    "pa-031": { optionEdits: { 0: "A wiki page documenting the required security standards for every team to follow" } },
    "pa-074": { optionEdits: { 2: "One organization and a single shared environment, separated only by naming conventions" } },
    "pa-064": { optionEdits: { 2: "1=Runtime Manager, 2=Anypoint Visualizer, 3=API Functional Monitoring (BAT)" } },
    "pa-016": { optionEdits: { 2: "Let each API return the raw backend format and push all of the mapping onto its consumers" } },
    "pa-020": { optionEdits: { 0: "One API that connects to both Salesforce and Marketo and serves the web site directly in one flow" } },
    "pa-067": { optionEdits: { 0: "A policy-violation count threshold exceeded on a managed API instance" } },
    "pa-011": { optionEdits: { 3: "Neither integration is supported; Anypoint federates only its own built-in identity provider store" } },
    "pa-012": { optionEdits: { 0: "In the MuleSoft-hosted control plane region where the whole platform is administered" } },
    "pa-044": { optionEdits: { 2: "Private Cloud Edition, which is operationally identical to CloudHub in every respect" } },
    "pa-047": { optionEdits: { 0: "Re-running the same MUnit unit suite that already passed during the build stage" } },
    "pa-081": { optionEdits: { 1: "Let each process API query SAP directly to avoid an extra hop, then fix them up at migration" } },
    "pa-024": { optionEdits: { 3: "Whatever structure DataWeave happens to produce from the SAP response by default" } },
    "pa-027": { optionEdits: { 0: "Keep it as-is, since every call is expected to traverse all three API-led layers" } },
    "pa-032": { optionEdits: { 2: "All three enforcement points handled through a Flex Gateway deployed at the edge" } },
    "pa-037": { optionEdits: { 0: "Only at the Experience API, since the inner Process and System APIs trust it" } },
    "pa-055": { optionEdits: { 0: "None — CloudHub already provides high availability for this topology by default" } },
    "pa-073": { optionEdits: { 1: "Point-to-point integration, which keeps the number of APIs to an absolute minimum" } },
    "pa-038": { optionEdits: { 3: "Resource-level scoping is available only when you write a custom policy by hand" } },
    "pa-076": { optionEdits: { 1: "A resourceType — it defines a complete reusable resource together with its methods" } },
    "pa-030": { optionEdits: { 0: "Rate limiting, which hard-rejects any requests received over the configured limit" } },
    "pa-057": { optionEdits: { 2: "A larger CloudHub worker to serve the high read volume faster" } },
    "pa-066": { optionEdits: { 0: "Configuring more alerts across the layers simply signals a thorough operations team" } },
    "pa-070": { optionEdits: { 2: "It only runs in lower development environments and never against production" } },
    "pa-022": { optionEdits: { 0: "A new Process API running four Scatter-Gather branches to assemble the whole page on every request" } },
    "pa-019": { optionEdits: { 3: "Maintain one giant enterprise specification containing every API and all of its shared types" } },
    "pa-029": { optionEdits: { 3: "An instance only in production; the lower environments simply run unmanaged" } },
    "pa-046": { optionEdits: { 0: "Manual Anypoint Studio exports uploaded through Runtime Manager by hand each time" } },
    "pa-068": { optionEdits: { 0: "The total number of lines of DataWeave written across all of the projects" } },
    "pa-079": { optionEdits: { 1: "The experience API that serves the public web storefront channel" } },
    "pa-086": { optionEdits: { 1: "Publish the API's single shared API key on the public portal page for every consumer" } },
    "pa-091": { optionEdits: { 2: "Two identical applications with DNS manually switched over on any failure event" } },
    "pa-092": { optionEdits: { 1: "Publish the APIs without any documentation so that outsiders simply cannot find them" } },
    "pa-094": { optionEdits: { 1: "Set the outbound response timeout to 30 seconds to match the downstream SLA exactly" } },
    "pa-095": { optionEdits: { 2: "A firewall rule set, since all enforcement should happen at the network layer" } },
    "pa-033": { optionEdits: { 3: "Query parameters interfere with the gateway's rate-limiting counters and quotas" } },
    "pa-069": { optionEdits: { 0: "Worker CPU-utilization threshold alerts configured in Runtime Manager" } },
    "pa-077": { optionEdits: { 1: "Remove the field from v2 directly and email the consumers about the schema improvement" } },
    "pa-078": { optionEdits: { 2: "The API's MUnit test suite running in the delivery pipeline" } },
    "pa-058": { optionEdits: { 0: "An HTTP Caching policy applied across the whole API instance in API Manager" } },
    "pa-015": { optionEdits: { 3: "No version change is needed at all — response schema edits don't affect the contract" } },
    "pa-023": { optionEdits: { 0: "When aggregating read-only reference data across several existing REST APIs" } },
    "pa-093": { optionEdits: { 3: "Asking consumers to retry any failed requests during the deployment window themselves" } }
  }
};
