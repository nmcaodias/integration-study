/* MCD2 · reorder sections into the official Salesforce Certified MuleSoft
 * Developer II exam-outline order (Spring '24). The sections were originally
 * stored in weight-descending order (27/25/20/15/13), which is NOT the outline
 * order. Official outline sequence:
 *   1. Expose production-ready APIs (13%)          -> d5
 *   2. Maintainable & modular / Maven builds (25%) -> d2
 *   3. Monitorable Mule applications (15%)         -> d4
 *   4. Performant & reliable applications (27%)    -> d1
 *   5. Secure data at rest and in transit (20%)    -> d3
 * Section ids are unchanged (question/exercise FKs stay valid); only array
 * order changes, which is what the app renders by. */
module.exports = {
  sectionOrder: ["d5", "d2", "d4", "d1", "d3"]
};
